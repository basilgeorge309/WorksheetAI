# Scribbl — Session 37 Rubric (respond after solve, image-gen in EdgeRuntime.waitUntil)

Stop holding the HTTP connection open for 2+ min. Return 202 after Step-1 solve, run Step-2
image generation as a background task; the client learns the outcome from the DB row.

- [ ] **1. Synchronous phase intact + 202.** Pre-solve gates keep their exact status codes
      (auth 401, ownership 403, tier 429, size 413/422); Step-1 solve + no-questions 422 stay
      synchronous. On a good solve the handler returns
      `{ success:true, status:'processing', worksheetId }` at HTTP **202** (via `json`, CORS
      included) before any image work.
- [ ] **2. Step 2 extracted to `runImageGeneration(...)`.** One async fn: image gen → decode
      → pdf-lib wrap → upload → completion write (status/output_path/answer_count/
      handwriting_style, fail-loud on updErr) → usage upsert. Logic unchanged, only relocated.
- [ ] **3. Background execution.** `EdgeRuntime.waitUntil(runImageGeneration({...}))` then
      return 202; isolate stays alive to finish (~130s < 150s limit). `EdgeRuntime` declared
      for TS.
- [ ] **4. Background error handling.** `runImageGeneration` has its OWN try/catch writing
      `status='error'` + `error` (≤500 chars) — the outer catch can't (response already sent).
- [ ] **5. Client compatibility + signed-URL TTL.** 202 works with `pollInBackground` (polls
      DB) and `fillWorksheet` retry (fast-path skipped → DB poll). Signed URL TTL 300→**600s**.
- [ ] **6. Scope + deploy.** Only `fill-worksheet/index.ts` changes; deploy via the given
      command; real run returns ~10s, row flips to `complete` ~130s later.

## Out of scope
- No client / design-system / remote-push changes.
- `answer_count` column assumed present (prior session's outstanding migration).
