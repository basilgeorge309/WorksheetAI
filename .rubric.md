# Scribbl — Session 8 Rubric (History Screen)

Five pass/fail checks. A real, refreshable list of the user's worksheets.

## Checks

- [ ] **1. Real data loads, newest-first, capped at 20.** `history.tsx` fetches
      the signed-in user's `worksheets` (id, created_at, storage_path,
      output_path, status, style, difficulty, subject) ordered by `created_at`
      desc, limit 20, into typed `Worksheet[]`. `useFocusEffect` re-fetches on
      every tab focus.

- [ ] **2. Rows render to spec and are tappable.** Each row: document-outline
      icon (24, #6B6B6B), filename = `storage_path` basename (15/600/#1A1A1A,
      1 line), subtitle = `formatDate · Style · Difficulty` (13/#6B6B6B), and a
      status badge. Rows use `TouchableOpacity` (activeOpacity 0.7) and navigate
      to `/worksheet/{id}?outputPath={output_path}&status={status}`. Separator is
      a 1px #E5E5E5 `ItemSeparatorComponent`. Every `Text` is `selectable={false}`.

- [ ] **3. Status badges match the config.** complete→"Done" (#DCFCE7/#16A34A),
      processing→"Working…" (#DBEAFE/#2563EB), pending→"Pending" (#FEF9C3/#CA8A04),
      error→"Failed" (#FEE2E2/#DC2626); badge padding 8/4, radius 12, size 12,
      weight 600.

- [ ] **4. Loading / empty / error / pull-to-refresh states.** Loading shows 5
      animated skeleton rows (opacity 0.4↔0.8 loop). Empty shows time-outline
      (48/#D1D5DB) + "No worksheets yet" + the upload hint. Error shows
      "Couldn't load history" (#DC2626) + a "Try again" button that re-fetches.
      `RefreshControl` (tint #2563EB) drives `refreshing` and re-fetches.
      `contentContainerStyle` adds `paddingBottom: 100`.

- [ ] **5. Worksheet screen handles non-complete + compiles/scoped.**
      `worksheet/[id].tsx` reads a `status` param; when `status !== 'complete'`
      and there's no `outputPath`, it shows the hourglass "Still working on it"
      state with a "Go back" button (no crash, no blank WebView). `tsc --noEmit`
      passes (strict), iOS bundle exports clean, zero inline styles, no new
      colors, and auth / edge function / home screen are untouched.

## Explicitly out of scope this session
- No search/filter, swipe-to-delete, sharing-from-history, or pagination past 20
- No auth / edge function / home screen changes
