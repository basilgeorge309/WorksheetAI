# Scribbl — Session 15 Rubric (Nav header restyle + History stuck-processing)

Five checks. Behavior preserved except the targeted header + stalled-retry additions.

- [ ] **1. Tab headers de-duplicated.** The native header on the tab screens
      (Home/History/Settings) no longer shows a title (so "History" stops appearing
      twice) — via `headerTitle: ''` (or `headerShown: false`) — with `colors.paper`
      background + `headerShadowVisible: false`. The in-content serif title is the
      only visible title; tab-bar labels still read Home/History/Settings.

- [ ] **2. Worksheet detail header restyled + de-duplicated.** The
      `worksheet/[id]` Stack.Screen uses `headerStyle: { backgroundColor:
      colors.paper }`, `headerShadowVisible: false`, serif-italic
      `headerTitleStyle`, `headerTintColor: colors.ink`, and a custom
      `headerLeft` bordered back button (32×32, `radius.md`, 1.5px ink border,
      `arrow-back`) wired to `router.back()`. The screen's in-content
      chevron-back `Pressable` is removed (no duplicate back affordance). Back
      still works.

- [ ] **3. Stuck-processing root cause investigated + polling verified.**
      `fillWorksheet()` polling confirmed correct (every 2s, max 30s, resolves on
      status ≠ pending/processing). Findings reported: the 3 stuck rows are
      orphaned (the edge function never reached `complete`/`error` for them — most
      likely pre-auth-fix test data), so they can never self-resolve and need the
      stalled UI + manual cleanup.

- [ ] **4. History "Stalled" state + retry.** A row in `pending`/`processing`
      whose `created_at` is older than 5 minutes renders a **"Stalled"** badge and
      a **Retry** control that re-invokes `fillWorksheet(id, storage_path,
      handwriting_style, difficulty, subject)` (with a per-row loading state and
      error surfaced inline, no crash). Fresh in-flight rows (< 5 min) still show
      "Working…". Tapping retry re-fetches on completion.

- [ ] **5. Cleanup SQL + clean build, scoped.** The cleanup `DELETE` SQL for the
      stuck rows is printed. `tsc --noEmit` passes; iOS bundle exports clean;
      Confetti/celebration logic and the fill-worksheet **core** logic are
      unchanged (only debug/investigation); no Home/Settings layout changes beyond
      the header fix.

## Out of scope
- No Confetti/celebration changes; no edge-function core rewrite; no Home/Settings
  layout changes beyond the header.
