# Scribbl — Session 20 Rubric (Sign-up redesign + Home usage bar)

Visual-only on StepFreetier (handlers/state unchanged) + a new UsageBar swapped
into Home. No other screens, no auth-logic, no navigation changes.

- [ ] **1. Sign-up header.** StepFreetier top is a centered header separated by a
      bottom hairline (`colors.cardBorder`): a centered pencil glyph (~44–56px),
      "Welcome to Scribbl" in `type.displaySerif` (~26px) centered, and the tagline
      "Your handwriting, done in seconds." in `colors.graphite` centered (~6px gap).
      *(Pencil rendered as the ✏️ glyph — `react-native-svg` was removed in S18 and
      there's no bare-pencil PNG asset; the emoji matches the icon art. Flagged.)*

- [ ] **2. Social buttons + divider.** Apple button: `colors.ink` bg, paper text,
      `radius.md`, `shadow.button`, label "Continue with Apple". Google button: paper
      bg, `border.hairline`, `colors.ink` text, `radius.md`, label "Continue with
      Google". Between them and the email section: an "OR" divider — `colors.cardBorder`
      hairlines on both sides, centered small uppercase "or"/"OR".

- [ ] **3. Inputs, primary, toggle, footer.** Small-caps "EMAIL"/"PASSWORD" labels
      (`type.label`/`mutedText`) above bordered inputs (`border.hairline`, `radius.md`,
      paper bg; password `secureTextEntry`). Primary button: `colors.ink`, `radius.md`,
      `shadow.button`, serif-italic label that switches **"Create my account →"**
      (sign-up) / **"Sign in →"** (sign-in) by `isSignUp`. Toggle row "Already have an
      account? **Sign in**" (graphite text, actionable part `colors.ink` bold).
      Footer: centered 11px `colors.mutedText` "By continuing, you agree to our Terms
      and Privacy Policy" with **Terms**/**Privacy Policy** underlined + tappable,
      opening the existing legal URLs (same as settings.tsx) via `Linking`.

- [ ] **4. Sign-up logic fully intact.** No change to `signInWithApple/Google`,
      `signUpWithEmail/signInWithEmail`, `runProvider`, `handleSuccess`, the `isSignUp`
      toggle, or `onNext` completion. Per-button loading spinners during auth, inline
      `colors.errorRed` errors + info text, and silent `'cancelled'` OAuth handling all
      still work. Inputs disabled while busy.

- [ ] **5. UsageBar component (correct burn-down).** New `components/UsageBar.tsx`:
      `limit` segments where segment `i` is filled `colors.ink` when `i >= used`, else
      `colors.cardBorder`; `isPro` → a single full `colors.successGreen` bar. Verified:
      used=0 → [ink,ink,ink]; used=1 → [cardBorder,ink,ink]; used=2 →
      [cardBorder,cardBorder,ink]; used=3 → [cardBorder,cardBorder,cardBorder]
      (first segments empty first → reads left-to-right as burning down).

- [ ] **6. Home swap only.** The usage **text caption** under "Scribbl" is replaced
      by `<UsageBar used={usage?.used ?? 0} limit={usage?.limit ?? 3} isPro={usage?.isPro} />`
      with existing spacing preserved; any now-dead `usageColor`/`remaining` cleaned up.
      Nothing else on Home changes (source picker, preview, style/difficulty, Fill
      button). `tsc --noEmit` passes; iOS bundle exports clean.

## Out of scope
- No auth-logic/handler changes; no other onboarding steps; no Home changes beyond
  the usage indicator; no new screens/navigation; no design-token changes.
