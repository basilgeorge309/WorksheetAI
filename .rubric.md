# Scribbl — Session 14 Rubric (Notebook v2 + History bug fix)

Eight checks: 1 bug fix + 7 screens. v2 = cleaner notebook (left-margin accent
instead of ruled grid, radius.md default, hairline card borders, shadow.button
on primary buttons). Behavior/logic/navigation unchanged.

- [ ] **1. History bug fixed.** Root cause confirmed via the debug log
      (PostgREST `42703` — `column worksheets.style does not exist`). The
      `fetchHistory` SELECT, the `Worksheet` type, and `getSubtitle` use
      `handwriting_style` (the real column), not `style`. History then loads real
      rows (or a clean empty state) with no error. Debug `console.log`s removed.

- [ ] **2. theme v2 + RuledBackground removed.** `constants/theme.ts` replaced
      with the v2 tokens (colors incl. `cardBorder`/`marginRed`, radius
      sm6/md8/lg12/pill, `border.hairline`(cardBorder)/`border.rule`,
      `shadow.button`, type incl. `buttonSerif`/`displaySerif`28→30). 
      `components/RuledBackground.tsx` is **deleted** and **no `<RuledBackground/>`
      usage remains anywhere**. Screens use `colors.paper` bg + a single
      `borderLeftWidth: 2, borderLeftColor: colors.marginRed` on the content
      container. No ruled horizontal lines, no `radius.sharp` (2px) anywhere.

- [ ] **3. Home.** `displaySerif` title; usage small-caps green/alertRed; three
      **individual** bordered source buttons (PDF/Camera/Photos, icon+label, no
      dashed box); segmented style + difficulty controls (`border.rule` container,
      `radius.md`, 1.5px ink dividers, ink-fill selected); "Fill it in →" =
      ink fill + `radius.md` + `shadow.button` + `buttonSerif`.

- [ ] **4. Onboarding (5 steps + ProgressDots + OnboardingButton).** paper +
      left accent; `displaySerif` headlines; option cards `radius.md` +
      `border.hairline` → ink fill selected; ProgressDots squares
      (ink active / cardBorder inactive); OnboardingButton ink + `shadow.button` +
      `radius.md`.

- [ ] **5. Worksheet celebration.** RuledBackground gone; Confetti + spring logic
      unchanged (colors stay pencilYellow/alertRed/ink); cards/buttons `radius.md`;
      Download = ink + `shadow.button`; Share secondary bordered; title
      `displaySerif`, subtitle `bodySerif`.

- [ ] **6. History styling.** paper + left accent; row cards `radius.md` +
      `border.hairline` (no sharp corners); badges `radius.sm` bordered with
      label text; empty state = dashed-circle (`border.hairline` + dashed,
      `radius.pill`) around the icon.

- [ ] **7. Settings.** `type.label` section labels; ACCOUNT bordered row;
      SUBSCRIPTION plan row bordered + ink Upgrade button + plain Restore link;
      LEGAL = one bordered `radius.md` container, 3 hairline-divided rows
      (Terms/Privacy/Support) each with a chevron; Sign out bordered (red text);
      Delete Account = plain red text below, no border.

- [ ] **8. Modals + Tab bar.** Modal sheets `radius.lg`, internal buttons
      `radius.md`, ink primary + `shadow.button`. Tab bar `colors.paper` bg,
      active `ink` / inactive `mutedText`, no underline clutter.

## Cross-cutting
- Zero hardcoded hex in touched files (theme.ts only; rgba backdrops + #000000
  Apple button excepted). `tsc --noEmit` passes; iOS bundle exports clean;
  logic/nav/state diff = 0; Confetti timing/physics untouched.

## Out of scope
- No logic/API/navigation changes; no ruled lines; no sharp 2px corners.
