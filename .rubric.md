# Scribbl — Session 27 Rubric (Quality fix verify + tiered cost caps)

- [ ] **1. Part 1 A (prompt) present.** Image-edit prompt explicitly separates
      EXISTING PRINTED (questions quoted verbatim, "do not touch/redraw, if unsure
      leave it") from NEW pencil to ADD. (S26 version confirmed.)
- [ ] **2. Part 1 B (quality).** `quality:'high'`; `input_fidelity:'high'`,
      `size:1024x1536`, `gpt-image-1.5` kept; image-gen log shows a ~$0.13-0.16 cost.
- [ ] **3. Migration.** `supabase/migrations/20260630_usage_cost_caps.sql` adds
      `cost_cents integer not null default 0` to `public.usage`; printed for manual run.
- [ ] **4. Client tier resolution.** `getUserTier(): 'free'|'pro'|'max'` from
      `entitlements.active['max'|'pro']`; `identifyRevenueCatUser(userId)` (`logIn`)
      wired in `_layout.tsx`; RC-dashboard setup documented as manual follow-up.
- [ ] **5. Edge tier gating.** `TIER_LIMITS` (free count 3 / pro 1000¢ / max 5000¢);
      gate BEFORE image gen by the resolved tier's cap; clear tier-appropriate 429.
- [ ] **6. Cost recording.** On success, upsert `worksheets_used+1` AND
      `cost_cents += ESTIMATED_COST_CENTS` (16), re-read first to avoid clobber.
- [ ] **7. Security — server-side, fail-closed.** Tier resolved ONLY server-side via
      RevenueCat REST (sk_ secret); never trusts client-passed tier; missing key/error/
      no entitlement → **'free'** (no bypass). Manual follow-ups documented.
- [ ] **8. Client UX + build.** `checkUsage` returns tier + cap info; `UsageBar`:
      free=segmented count, pro/max=dollar progress; Home passes new fields. `tsc` 0;
      iOS bundle clean; edge fn deploys + 4xx smoke.

## Out of scope
- No Step-1 changes; no RevenueCat dashboard config; no paywall pricing display changes.
