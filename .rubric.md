# WorksheetAI — Session 5 Rubric (PDF Positioning Fix + RevenueCat Paywall)

Six pass/fail checks. Part 1: answers land ON the worksheet. Part 2: paywall on
exhausted free tier. Must never crash in Expo Go.

## Checks

- [ ] **1. Edge function positioning rewritten + redeployed.**
      `fill-worksheet/index.ts` reads real page size via `firstPage.getSize()`,
      uses `TOP_MARGIN 80 / BOTTOM_MARGIN 60 / LEFT_MARGIN 72 / RIGHT_COLUMN_X 320`,
      distributes answers in two columns with the y-coordinate inverted from the
      top (`y = height - TOP_MARGIN - …`), passes `maxWidth: 220`, draws a
      "Alex Johnson" name near the top-right (`x = width-180`, `y = height-58`),
      and supports multi-page (12 answers/page). Column choice uses `x_hint`
      (left→72, right→320) when present. The function is redeployed to
      `wvoffvyhrkkfpgzlzcky` and still returns a structured response.

- [ ] **2. Anthropic prompt asks for `x_hint` + `index`.** The system prompt now
      requests the updated format
      `[{question, answer, position, x_hint:"left|right", index:N}]`, and the
      parser/positioning tolerate missing `x_hint`/`index` (fall back gracefully).

- [ ] **3. `lib/revenuecat.ts` is Expo-Go-safe.** Exports `initRevenueCat`,
      `getCustomerInfo`, `isProUser`, `purchasePro`, `restorePurchases`.
      `react-native-purchases` is **lazy-imported** inside a try/catch (never at
      module top level), every native call is guarded, and in Expo Go each
      function returns a safe fallback (`null` / `false` / `{success:false}`)
      with no throw. `'cancelled'` is preserved as a distinct, non-error signal.

- [ ] **4. `PaywallModal.tsx` matches spec.** Bottom-sheet Modal (slides up, drag
      handle, feature checklist with #2563EB checks, "$4.99 / month" + annual
      line, Start Pro + Try Annual + Restore + Maybe later). Buttons disable +
      show `ActivityIndicator` while a purchase is in flight; errors render inline
      in #DC2626; `'cancelled'` shows nothing; in Expo Go "Start Pro" shows
      "Upgrade available in the full app." instead of crashing. `onSuccess`
      closes + signals a usage refresh; `onClose` via Maybe later.

- [ ] **5. Wiring: checkUsage / home / settings / startup.** `checkUsage` returns
      `{used, limit, canUse, isPro}` — pro → `canUse:true`, `limit:Infinity`.
      Home shows the **PaywallModal** (not an inline error) when an exhausted user
      taps "Fill it in →", re-checks usage on `onSuccess`, and displays
      "Pro — Unlimited" for pro users (else "X of N remaining"). `_layout.tsx`
      calls `initRevenueCat()` on startup. Settings has a SUBSCRIPTION section
      (Free/Pro badge, Upgrade→paywall when free, Restore purchases) above Sign out.

- [ ] **6. Compiles, safe, deployable.** `tsc --noEmit` passes (strict); iOS
      bundle exports with zero resolution errors; **no top-level import of
      react-native-purchases / -ui anywhere** (grep-clean) so Expo Go can't crash;
      `ANTHROPIC_API_KEY` stays server-side only; zero inline styles in touched RN
      files. The RevenueCat dashboard + `.env.local` key-rename checklist is
      printed.

## Explicitly out of scope this session
- No real annual purchase flow (annual button wired to monthly package for MVP)
- No family sharing, no web paywall, no promo-code UI
- No `isPro` DB column (pro is checked client-side via RevenueCat)
