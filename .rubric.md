# WorksheetAI — Session 6 Rubric (Show Work + Pencil Effect)

Four pass/fail checks. Scope: `supabase/functions/fill-worksheet/index.ts` ONLY.

## Checks

- [ ] **1. Prompt asks for full working steps (multi-line).** The Anthropic
      prompt text is replaced with the "show your full working steps" version:
      per-style step detail (neat/average/messy), per-difficulty behavior, the
      CRITICAL math/algebra/word-problem guidance, "→ between steps", "under 4
      lines", and the JSON format whose `answer` contains `\n`-separated working
      (e.g. `"step1\n→ step2\n→ final answer"`) plus `x_hint` + `index`.

- [ ] **2. Multi-line answers render with height-aware stacking.** A
      `drawMultilineAnswer(page, text, startX, startY, font, fontSize, color)`
      helper splits on `\n`, uses `lineHeight = fontSize * 1.4`, skips lines with
      `y <= 40`, and draws each line (maxWidth 180). Both left and right columns
      accumulate downward from `topY`: each block consumes
      `lines * fontSize * 1.4`, with 8px padding between blocks, and is skipped
      when `currentY - blockHeight <= bottomY`.

- [ ] **3. Pencil effect applied.** Pen blue `rgb(0.1,0.1,0.5)` is gone for
      answers; answers use a gray pencil color. Per-answer font-size variation
      (`baseFontSize 9.5` + up to ~1.5 random → 9.5–11.0) and per-answer color
      variation via a 3-shade `pencilColors` array (`getPencilColor(index)`). The
      "Alex Johnson" name uses pencil gray `rgb(0.25,0.25,0.25)`, size 11, and the
      handwriting `answerFont`.

- [ ] **4. Compiles, deploys, scoped.** Function redeploys to
      `wvoffvyhrkkfpgzlzcky` and the smoke test (empty POST) still returns the
      structured `{success:false,...}` 400. No React Native files, schema, font
      URLs, or JSON-parsing logic changed — diff is confined to this one file.

## Explicitly out of scope this session
- No RN file changes, no schema changes
- No font URL changes (Caveat/Architects Daughter stay)
- No OCR / image processing
- No changes to the JSON parsing logic
